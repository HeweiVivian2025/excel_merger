services:
  - type: web
    name: excel-merger
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PORT
        value: 10000
    disk:
      name: upload-disk
      mountPath: ./uploads